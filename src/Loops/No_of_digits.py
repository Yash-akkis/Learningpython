'''
Number of Digits: Create a program that counts the number of digits in a number.
The number should be input by the user.
'''

user_input = input('enter the number')
user_string = str(user_input)
length = len(user_string)

print(length)